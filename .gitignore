# Build output
/build*/
/temp

# Compiled binaries (built via CMake into build/, not committed)
client/client
tracker/tracker

# Course material - kept locally, deliberately not published
AOS-Assignment-3.pdf
*_Assignment*.pdf

# Superseded by docs/ - described the pre-rewrite version of the project
Report.pdf

# Editor / tooling
/.vscode
/.history

# Runtime state produced by running the tracker/client
*.downloading
tracker_state*.db
tracker_state*.db.tmp

/docs/
/.github/
